
using Xamarin.Forms;
namespace Seek.Controls.Temp
{
    /*
    public class ImageButton : Xamarin.Forms.ImageButton, IColorized
    {
        // aka TintColor 
        public Color Color { get => (Color)GetValue(ColorProperty); set => SetValue(ColorProperty, value); }
        public static BindableProperty ColorProperty =
            BindableProperty.CreateAttached("Color", typeof(Color), typeof(ImageButton), new Color(255, 255, 255));
    }
    */
}

// remove this as it is only used by settings button, unless needing it in future buttons? 